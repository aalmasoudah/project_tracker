# Worker-only secret. Store the real value in the platform secret manager.
# Do not attach this environment to web, scheduler, release, or n8n services.
GROQ_API_KEY=replace-with-the-separate-staging-worker-key

# Vendored Frontend Assets

The application serves pinned frontend dependencies locally:

- Bootstrap 5.3.8 LTR, RTL, and bundle assets.
- htmx 2.0.9.
- Noto Sans Arabic 2.013 variable font.

Source licenses are stored beside each dependency. Update versions only through
an explicit dependency review and rerun Arabic/English browser verification.
